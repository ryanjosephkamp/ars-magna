# Update the judge routine

Claude Code only. Paste everything below the line into a session opened in the repository, after the
change to `automation/judge-routine.md` is merged to `main`. No placeholders.

---

automation/judge-routine.md changed on main. Carry it over to the judge routine trig_01VdomdgqWVjsNdo33BHQFNg: follow "Update the routine" in docs/OPERATOR.md. The new prompt is the file's text above its closing HTML comment, exactly. Change nothing else about the routine: not its schedule, model, tools or repository. Then read the routine back, confirm its prompt matches the file line for line, and report its next run time.
