# Check a rotated secret

Paste everything below the line into an agent session opened in the repository, after you have set the
new value yourself with `gh secret set`. No placeholders.

---

I have just rotated a repository secret. Read AGENTS.md and "Rotate a secret" in docs/OPERATOR.md. Without reading, printing or asking for any secret's value: list the repository secrets with their updated dates, name the one that changed most recently, run the workflow that uses it, and report whether that run passed. Stop there.
