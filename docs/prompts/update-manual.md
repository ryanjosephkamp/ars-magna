# Update the operator manual

Replace task_description with what changed in how the site is operated, then paste everything below the
line into an agent session opened in the repository.

---

Update the Ars Magna operator manual: task_description. Read AGENTS.md, then follow "Update this manual" in docs/OPERATOR.md: change docs/OPERATOR.md and any template in docs/prompts/ that the change touches, in one pull request, run the four suites, open it, report CI, and stop. After I merge, republish the manual's artifact to the URL recorded in its last section.
