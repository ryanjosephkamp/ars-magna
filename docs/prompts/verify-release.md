# Verify a release

Replace pr_number with the pull request that was merged, then paste everything below the line into an
agent session opened in the repository.

---

Pull request pr_number is merged. Read AGENTS.md, then run every check in "Verify a release" in docs/OPERATOR.md against it and report each one as passed or failed, with the line of output that shows it. Change nothing. If a check fails, say what failed and what you would do about it.
