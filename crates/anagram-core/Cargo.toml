[package]
name = "anagram-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Multi-word anagram search: rarest-letter run-forced DFS over signature classes."

[dependencies]

[dev-dependencies]
proptest = "1.8"
# Only a test oracle: the shipped fold is a generated table, because these
# tables would add ~50 KB to the compressed WASM payload.
unicode-normalization = "0.1"

[lib]
crate-type = ["rlib"]
