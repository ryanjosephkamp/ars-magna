{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://ars-magna.pages.dev/schema/acronym.schema.json",
  "title": "Acronym",
  "description": "One term of the acronyms class (decision D63): an initialism or a text abbreviation that is not a word of the dictionary. One line of data/vocabulary/acronyms.jsonl. The search admits it only when the class is on, and every anagram carries the class of each such term; words alone is the default everywhere. An initialism English OpenList already carries as a word (lol, faq, ok) needs no class and is refused. Every term is public, carries what it reads as, a meaning a reader can read and a public trace; the operator admits it by merging the pull request that adds it, and the rebuilt classes artifact is committed with [dict].",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "term",
    "reads_as",
    "gloss",
    "trace",
    "proposed_by",
    "added"
  ],
  "properties": {
    "term": {
      "type": "string",
      "pattern": "^[a-z]{2,45}$",
      "description": "Lowercase letters only, as the search folds them: wtf, btw, thx. How it is usually written, when that is capitals, goes in `written`."
    },
    "reads_as": {
      "type": "string",
      "minLength": 1,
      "maxLength": 80,
      "pattern": "^[a-z][a-z' -]*$",
      "description": "What the term stands for when it is read: the word or words it reads as (`and` for `&`, `you` for `u`, `bait` for `b8`, `by the way` for `btw`). Lowercase; a reader sees it in the word panel beside the gloss."
    },
    "gloss": {
      "type": "string",
      "maxLength": 160,
      "pattern": "^[A-Z][^\\n]*\\.$",
      "description": "What the term means, in one sentence. A reader sees this in the word panel, so PRODUCT.md's rule applies: a capital letter at the start and a full stop at the end."
    },
    "trace": {
      "type": "string",
      "pattern": "^https?://",
      "description": "Where the term is attested, always public: its Wiktionary entry, or another page anyone can open."
    },
    "proposed_by": {
      "type": "string",
      "minLength": 1,
      "description": "Who proposed it. A model or the pipeline may propose; only a merge admits."
    },
    "added": {
      "type": "string",
      "pattern": "^\\d{4}-\\d{2}-\\d{2}$"
    },
    "tone": {
      "type": "string",
      "enum": [
        "crude"
      ],
      "description": "`crude` when the term or what it reads as is an expletive or a slur. A crude term's class is off by default, and the standing rules on anagrams of living people apply to it."
    },
    "also": {
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": {
        "type": "string",
        "enum": [
          "symbols",
          "shorthand",
          "blends",
          "acronyms",
          "slang",
          "names"
        ]
      },
      "description": "The other classes this term is listed in, when it is meant to be in more than one; the build gives it every class's bit. Without this, a term in two files is a mistake vocab:check reports."
    },
    "note": {
      "type": "string",
      "description": "Anything else worth recording about the admission."
    },
    "written": {
      "type": "string",
      "minLength": 2,
      "maxLength": 10,
      "pattern": "^[A-Za-z]+$",
      "description": "How the term is usually written when that is not the lowercase term: `WTF`, `RSVP`. Discover shows an acronym this way (D63); a text abbreviation such as `thx` has none. Its letters are the term's."
    }
  }
}
